import 'package:flutter/material.dart';

class Contactanos extends StatelessWidget {
  static final String routeName = 'contactanos';


  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Contáctanos'),
      ),
    );
  }
}