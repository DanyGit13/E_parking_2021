import 'package:flutter/material.dart';

class MetodoPago extends StatelessWidget {
  static final String routeName = 'metodo_pago';


  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('metodo pago'),
      ),
    );
  }
}
